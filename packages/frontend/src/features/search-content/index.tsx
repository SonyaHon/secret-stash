import React from "react";
import { observer } from "mobx-react";
import { Box } from "@mui/material";

export const SearchContent: React.FC = observer(() => {
  return <Box>Search content</Box>;
});
